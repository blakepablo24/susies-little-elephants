{% extends 'admin/base.html.twig' %}

{% block main %}

    <main>
        
        <h2 class="main-title">Add Child to the {{family.name}} Family</h2>

            {{form_start(form, {'attr': {'novalidate':'novalidate', 'class': 'edit-family-form'}})}}
                
                

            {{form_end(form)}}

    </main>

{% endblock %}